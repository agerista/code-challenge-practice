class Tree(object):

    def __init__(self, data):
        self.root = root

class Node(object):
    
    def __init__(self, data):
        self.data = data
        self.left_child = []
        self.right_child = []


    def binary_tree(root):

        return [root, [], []]

    def insert_left(root, node):

        t = root.pop(1)
        if len(t) > 1:
            root.insert(node)

    def build_tree(node):
        """Write a function buildTree that returns a tree using the list of lists
        functions that looks like this:

                        a
                      /   \
                    b       c
                  /   \   /   \
                       d e     f
        

        >>> build_tree()
        ['a', ['b', [], []], ['c', [], ['d', ['e', [], []], []]]]
        """



    